import styled from "styled-components";

export const Top = () => {
  return (
    <div>
      <SContainer>
        <h2>TOPページです</h2>
      </SContainer>
    </div>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
